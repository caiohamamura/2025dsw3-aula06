# F1: criar ambiente
# F1: sele terminal -> clica engrenagem -> duplo clique -> associa tecla
# Criar o requirements com django

echo "django" > requirements.txt

pip install -r requirements.txt
pip freeze > requirements.txt
django-admin startproject aula06 .
python manage.py startapp instacaio

python manage.py createsuperuser
python manage.py makemigrations
python manage.py migrate
python manage.py runserver 9090

echo "pillow" >> requirements.txt


git init
echo ".venv" >> .gitignore
git config user.name "usuario"
git config user.email "usuario@teste.com"

git add .
git commit -a -m "Adicionei comandos git"
